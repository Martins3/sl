#ifndef STRATEGY_HPP
#define STRATEGY_HPP

#include "Word.hpp"

/**
 * define a bunch of functions to review
 */
class Strategy{
    static std::vector<Word> Ebbinghaus(std::vector<Word> & database);
};
#endif
